# Default
# https://code.claude.com/docs/en/skills
# Universal Command Line Interface for Claude CLI

alias claude.kill="pkill -f 'claude'"
alias claude.kill.strict="ps aux | grep '/opt/homebrew/bin/claude' | grep -v grep | awk '{print \$2}' | xargs kill"
alias claude.ps="pgrep -l -f 'claude'"
