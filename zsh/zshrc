# https://raw.githubusercontent.com/ivankatliarchuk/dotfiles/master/zsh/zshrc
# This file is intentionally minimal. All setup logic (PATH, debug, functions,
# aliases, starship, etc.) lives in settings/bootstrap.zsh — add new init
# there, not here.
[[ -f ${HOME}/.zsh/settings/bootstrap.zsh ]] && source ${HOME}/.zsh/settings/bootstrap.zsh
